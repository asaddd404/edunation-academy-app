# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
backend/.env
backend/uploads/

# Node / frontend
node_modules/
frontend/dist/
frontend/.env
*.tsbuildinfo

# Env
# `.env` alone does not match `.env.prod`, which holds the live database
# password and JWT secret -- ignore every .env* and re-allow the templates.
.env
.env.*
!.env.example
!.env.prod.example
!.env.staging.example

# Load-test fixtures are generated, not stored: 355 KB of binary that
# scripts/make_heavy_pdf.py rebuilds in a second.
scripts/fixtures/

# Logs
*.log

# OS / editor
.DS_Store
.idea/
.vscode/

# Docker
pgdata/
